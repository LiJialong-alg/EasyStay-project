# Hotel Management System Upgrade Plan (v2.1)

Based on your requirements, I will upgrade the existing system with improved visuals and detailed functional modules.

## 1. Global Visual & Layout Upgrade

### 1.1 Login Page (Visual Upgrade)

* **File**: `src/layout/AuthLayout.css`, `src/pages/auth/Login.jsx`

* **Changes**:

  * Implement immersive full-screen background.

  * Refine Glassmorphism effect: Deep blue/orange tint, low opacity, high blur (`backdrop-filter: blur(20px)`).

  * Add delicate border (`1px solid rgba(255, 255, 255, 0.2)`).

### 1.2 Merchant Layout (Sidebar)

* **File**: `src/layout/MerchantLayout.jsx`, `src/layout/MerchantLayout.css`

* **Changes**:

  * Change Sidebar background to Pure Black (`#000000`) or Dark Grey (`#141414`).

  * Set menu selection highlight color to Gold or Brand Blue for premium feel.

## 2. Function Module Implementation

### 2.1 Order Management

* **Structure**: Create `src/pages/merchant/order/`

* **Sub-modules**:

  1. **Order List (`OrderList.jsx`)**:

     * **Tabs**: Pending | Check-in | All | New.

     * **Filter Bar**: Keyword search + Date Range Picker buttons.

     * **Split Layout**: Left (List with scroll) + Right (Fixed Details Panel).
  2. **Reception Settings (`ReceptionSettings.jsx`)**:

     * Auto-accept time range, Max daily orders, Master switch.

### 2.2 Room Status Management

* **Structure**: Create `src/pages/merchant/room/`

* **Sub-modules**:

  1. **Room Status Calendar (`RoomStatusCalendar.jsx`)**:

     * X-Axis: Future 10 days; Y-Axis: Room Types.

     * Visuals: Red (Sold Out) / White (Available).

     * Interaction: Click cell to pop up settings (Price, Open/Close, Hourly/Full-day).
  2. **Price Adjustment (`PriceAdjustment.jsx`)**:

     * Grid layout of hotels.

     * Bulk update modal: Date range selection, Original vs Sale price.
  3. **Room Type Setup (`RoomTypeSetup.jsx`)**:

     * List view with Online/Offline Switch.

     * "Go Live" button for approved new rooms.

### 2.3 Marketing Module

* **Structure**: Create `src/pages/merchant/marketing/`

* **Sub-modules**:

  1. **Traffic Booster (`TrafficBooster.jsx`)**:

     * Before/After data comparison charts (Impressions, PV, Orders).

     * Traffic package purchase interface.
  2. **Campaigns (`Campaigns.jsx`)**:

     * 4 Fixed Cards: New User, Daily Deal, Student Discount (Upload logic), Consecutive Stay.

### 2.4 Review Management

* **File**: `src/pages/merchant/Reviews.jsx`

* **Enhancements**:

  * **Top Dashboard**: Total Score + Radar Chart (Location, Room, Hygiene, Service, Value).

  * **List Area**: Enhanced item design with Image Gallery support and Reply status.

### 2.5 Data Analytics

* **File**: `src/pages/merchant/Analytics.jsx`

* **Charts (ECharts)**:

  * **User Portrait**: Gender (Pie), Age (Bar), New/Old (Donut), Location (Bar).

  * **Booking Behavior**: Local/Foreign (Pie), Time Preference (Pie), Advance Booking (Stacked Bar), Stay Duration (Bar), Booking Time (Line/Heatmap), Room Count (Pie).

## 3. Execution Steps

1. **Styles**: Update global CSS and Layout styles first.
2. **Components**: Create reusable chart wrappers and layout components.
3. **Pages**: Implement new pages one by one (Order -> Room -> Marketing -> Reviews -> Analytics).
4. **Routing**: Update `App.jsx` to reflect new sub-menu structures.

